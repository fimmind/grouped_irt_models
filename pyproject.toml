[project]
name = "grouped-irt-models"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fasttext>=0.9.3",
    "numpy>=2.0",
    "openpyxl>=3.1",
    "pandas>=2.0",
    "scikit-learn>=1.4",
    "scipy>=1.10",
]

[tool.setuptools.packages.find]
where = ["src"]

